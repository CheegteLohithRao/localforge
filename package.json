{
  "name": "localforge",
  "displayName": "LocalForge",
"description": "A lightweight VS Code extension that connects user-owned, open-source Ollama models running locally.",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.107.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "keywords": [
    "ollama",
    "vscode",
    "local llm",
    "offline ai"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "localforge.explainSelection",
        "title": "LocalForge: Explain Selection"
      },
      {
        "command": "localforge.selectModel",
        "title": "LocalForge: Select Ollama Model"
      },
      {
        "command": "localforge.askWithSelection",
        "title": "LocalForge: Run Prompt on Selection"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "localforge.explainSelection",
          "when": "editorHasSelection",
          "group": "navigation"
        }
      ]
    },
    "keybindings": [
      {
        "command": "localforge.explainSelection",
        "key": "alt+M",
        "when": "editorHasSelection"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.107.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.48.1",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
